import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'plan-a-frontend-challenge-eduardo-alves',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
